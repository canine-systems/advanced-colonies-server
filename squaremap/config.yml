config-version: 2
settings:
    language-file: lang-en.yml
    debug-mode: false
    update-checker: true
    web-address: http://localhost:8080
    web-directory:
        path: web
        auto-update: true
    image-quality:
        compress-images:
            enabled: false
            value: 0.0
    internal-webserver:
        enabled: false
        bind: 0.0.0.0
        port: 8080
        flush-json-immediately: false
    ui:
        coordinates:
            enabled: true
        link:
            enabled: true
        sidebar:
            pinned: unpinned
    commands:
        main-command-label: squaremap
        main-command-aliases:
        - map
    render-progress-logging:
        enabled: true
        interval-seconds: 10
world-settings:
    default:
        map:
            enabled: true
            display-name: '{world}'
            order: 0
            icon: ''
            max-render-threads: -1
            iterate-up: false
            max-height: -1
            biomes:
                enabled: true
                blend-biomes: 3
            glass:
                clear: true
            lava:
                checkerboard: true
            water:
                clear-depth: true
                checkerboard: false
            zoom:
                maximum: 3
                default: 3
                extra: 2
            background-render:
                enabled: true
                # The maximum amount of chunks to render from the queue at
                # time. Setting this too high may cause lag on your main
                # thread if the queue gets too large
                #
                # default=1024, changing to 256 to reduce lag
                max-chunks-per-interval: 256
                interval-seconds: 15
                max-render-threads: -1
            markers:
                update-interval-seconds: 15
                spawn-icon:
                    enabled: true
                    show-controls: true
                    default-hidden: false
                    layer-priority: 0
                    z-index: 0
                world-border:
                    enabled: true
                    show-controls: true
                    default-hidden: false
                    layer-priority: 1
                    z-index: 1
            visibility-limits:
            -   type: world-border
                enabled: 'false'
        player-tracker:
            enabled: true
            # update-interval-seconds defaults 1s (yikes), but we go with 30s
            # because we only update the map each minute
            update-interval-seconds: 30
            show-controls: true
            default-hidden: false
            layer-priority: 2
            z-index: 2
            nameplate:
                enabled: true
                show-head: true
                heads-url: https://mc-heads.net/avatar/{uuid}/16
                show-armor: true
                show-health: true
            hide:
                invisible: true
                spectators: true
            use-display-names: false

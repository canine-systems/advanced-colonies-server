#!/usr/bin/env bash

if [ "$USER" != "minecraft" ]; then
    echo "Please run this script as the 'minecraft' user."
    exit 1
fi

echo "Generating SSH key..."
ssh-keygen -t ed25519 -C "minecraft@$HOSTNAME" || exit $?

echo "Copying ID to map.canine.systems..."
ssh-copy-id map.canine.systems
